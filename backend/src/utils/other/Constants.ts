export enum RoutePath {
  PRIVATE = '/private',
  PUBLIC = '/public',
}

export enum MeasuresOfLength {
  SMALL = 64,
  MIDDLE = 128,
  BIG = 256,
  LARGE = 512,
  GIANT = 1024,
  POEM = 2048,
  ROMAN = 4096,
}
